import SConsider

Import('*')

buildSettings = {
    packagename : {
        'public' : {
            'includeSubdir': 'source',
        },
    },
}

SConsider.createTargets(packagename, buildSettings)


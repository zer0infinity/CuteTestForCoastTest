from pkg_resources import require as pkg_require
pkg_require(["SConsider<0.5"])
import SConsider

Import('*')

buildSettings = {
    packagename: {
        'linkDependencies': ['testfw', 'CoastFoundation.AnythingOptional'],
        'sourceFiles': [],
        'targetType': 'IncludeOnly',
        'public': {
            'includes': SConsider.listFiles(['*.h']),
        }
    }
}

SConsider.createTargets(packagename, buildSettings)

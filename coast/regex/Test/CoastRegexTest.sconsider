import os, pdb
import SConsider
from stat import *

Import('*')

buildSettings = {
    packagename : {
        'includeSubdir'    : '',
        'linkDependencies' : ['CoastRegex', 'testfwFoundation'],
        'sourceFiles'      : SConsider.listFiles(['*.cpp']),
        'targetType'       : 'ProgramTest',
        'copyFiles'        : [( SConsider.listFiles( ['*.sh', 'config/*.any', 'config/LargeLiteralData.h'] ), S_IRUSR | S_IRGRP | S_IROTH )],
    }
}

SConsider.createTargets(packagename, buildSettings)
